---
import { sql } from '../../lib/db';

// Fetch all placement agent companies
const companies = await sql`
  SELECT
    id,
    name,
    slug,
    type,
    description,
    headquarters,
    logo_url,
    specializations,
    global_rank,
    created_at
  FROM companies
  WHERE (company_type = 'placement_agent' OR type = 'placement_agent')
    AND status = 'published'
  ORDER BY
    CASE WHEN global_rank IS NOT NULL THEN 0 ELSE 1 END,
    global_rank ASC NULLS LAST,
    name ASC
`;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Placement Agents Directory | Placement</title>
  <meta name="description" content="Comprehensive directory of leading placement agents and financial advisory firms">
</head>
<body class="bg-gray-50">
  <!-- Navigation -->
  <nav class="bg-white shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center">
          <h1 class="text-2xl font-bold text-blue-600">
            <a href="/">Placement</a>
          </h1>
        </div>
        <div class="flex items-center space-x-8">
          <a href="/" class="text-gray-700 hover:text-blue-600 font-medium">Home</a>
          <a href="/articles" class="text-gray-700 hover:text-blue-600 font-medium">Articles</a>
          <a href="/companies" class="text-blue-600 font-medium">Companies</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Page Header -->
  <div class="bg-gradient-to-br from-blue-600 to-blue-800 text-white py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-4xl md:text-5xl font-bold mb-4">Placement Agents Directory</h1>
      <p class="text-xl text-blue-100 max-w-3xl">
        Comprehensive profiles of leading placement agents and financial advisory firms in private equity, venture capital, and alternative investments.
      </p>
    </div>
  </div>

  <!-- Companies Grid -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {companies.length === 0 ? (
      <div class="text-center py-12">
        <p class="text-gray-600 text-lg">No companies found. Check back soon!</p>
      </div>
    ) : (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {companies.map((company) => (
          <a
            href={`/companies/${company.slug}`}
            class="bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow p-6 block"
          >
            <!-- Company Logo -->
            {company.logo_url && (
              <div class="mb-4 h-12 flex items-center">
                <img
                  src={company.logo_url}
                  alt={`${company.name} logo`}
                  class="h-10 w-auto"
                />
              </div>
            )}

            <!-- Company Name & Rank -->
            <div class="flex items-start justify-between mb-3">
              <h2 class="text-xl font-bold text-gray-900 flex-1">
                {company.name}
              </h2>
              {company.global_rank && (
                <span class="ml-3 inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-bold bg-blue-100 text-blue-800">
                  #{company.global_rank}
                </span>
              )}
            </div>

            <!-- Location -->
            {company.headquarters && company.headquarters !== 'Unavailable from provided context' && (
              <div class="text-sm text-gray-600 mb-3">
                📍 {company.headquarters}
              </div>
            )}

            <!-- Description -->
            {company.description && (
              <p class="text-gray-600 text-sm mb-4 line-clamp-3">
                {company.description}
              </p>
            )}

            <!-- Specializations -->
            {company.specializations && company.specializations.length > 0 && (
              <div class="flex flex-wrap gap-2">
                {company.specializations.slice(0, 3).map((spec: string) => (
                  <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-gray-100 text-gray-700">
                    {spec}
                  </span>
                ))}
                {company.specializations.length > 3 && (
                  <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium text-gray-500">
                    +{company.specializations.length - 3} more
                  </span>
                )}
              </div>
            )}
          </a>
        ))}
      </div>
    )}
  </div>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-12 mt-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <p class="text-gray-400">© {new Date().getFullYear()} Placement. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
